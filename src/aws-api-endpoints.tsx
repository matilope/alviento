/* import {Auth} from 'aws-amplify' */

const api_config = {
    API: {
      endpoints: [
        {
          name: "apibackend",
          endpoint: "https://api.*****.ar"
        }
      ]
    }
  }

export default api_config